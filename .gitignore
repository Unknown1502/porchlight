__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.local
.env.*.local
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
corpus/seed/*.json
eval/out/
.porchlight_store.json
.DS_Store

# Audit venv — local only, never committed.
.venv-audit/

# AWS evidence captured during verification; ids are account-specific.
deploy/evidence/

# Generated by `agentcore configure` — bakes in this account's specific ARNs,
# role names and resource ids. A judge running deploy/deploy_runtime.sh
# generates their own.
.bedrock_agentcore.yaml

# Transactional store — runtime state, never committed.
porchlight.db
porchlight.db-*

# Generated demo voiceover audio and video — a local recording aid, not source material.
docs/demo-narration/

